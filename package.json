{
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "cd frontend && vite",
    "buildfront": "cd frontend && tsc -b && vite build",
    "buildback": "cd backend && yarn start",
    "server": "cd backend && yarn server"
  }
}
